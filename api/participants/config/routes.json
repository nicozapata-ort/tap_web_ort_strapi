{
  "routes": [
    {
      "method": "GET",
      "path": "/participants",
      "handler": "participants.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/participants/count",
      "handler": "participants.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/participants/:id",
      "handler": "participants.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/participant",
      "handler": "participants.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/participants",
      "handler": "participants.register",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ranking",
      "handler": "participants.getRanking",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/participants/:id",
      "handler": "participants.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/participants/:id",
      "handler": "participants.delete",
      "config": {
        "policies": []
      }
    }
  ]
}